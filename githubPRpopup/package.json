{
  "name": "pr-inbox-extension",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "test": "jest --config jest.config.cjs",
    "test:unit": "jest --config jest.config.cjs",
    "test:e2e": "playwright test",
    "lint": "echo 'no linter configured'"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "playwright": "^1.35.0"
  }
}
